clear all;
clc;
close all;
v2_test_generate

%Initial checks
num_student = size(sdt_vec,1);
num_prof = size(prof_vec,1);
num_timeslot = size(population{1},1);
num_room = length(room_vec);
pop_fit = zeros(100,1);

%Fitness Testing
for h = 1:size(population,1)
    
fitness = zeros(1,num_student)
conflict_rating = [0 1:0.5:4];
collision_rating = [.1 .03 .015];
for i = 1:num_student
    sdt_timeloc = population{h}(sdt_vec)  
    fitness(i)popu = v2_sdt_fitness(sdt_timeloc,conflict_rating,collision_rating)
end
end

